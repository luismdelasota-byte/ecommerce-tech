spring.application.name=ecommerce-system
server.port=8087

# Perfil activo por defecto: dev (H2 en memoria para desarrollo local)
# Para produccion, usa: java -jar app.jar --spring.profiles.active=prod
spring.profiles.active=prod

debug=true
